<UserControl
    x:Class="OpenSilver.Samples.Showcase.SearchControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:OpenSilver.Samples.Showcase.Search">
    <UserControl.Resources>
        <ControlTemplate x:Key="CircleButtonTemplate" TargetType="Button">
            <Grid>
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="FocusStates">
                        <VisualState x:Name="Unfocused" />
                        <VisualState x:Name="Focused">
                            <Storyboard>
                                <ObjectAnimationUsingKeyFrames Duration="0"
                                                   Storyboard.TargetName="FocusVisual"
                                                   Storyboard.TargetProperty="Visibility">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="Visible" />
                                </ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                        </VisualState>
                    </VisualStateGroup>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="MouseOver">
                            <Storyboard>
                                <ColorAnimationUsingKeyFrames Duration="00:00:00"
                      Storyboard.TargetName="Hover"
                      Storyboard.TargetProperty="(Shape.Fill).(SolidColorBrush.Color)">
                                    <SplineColorKeyFrame KeyTime="0" Value="#55bababa" />
                                </ColorAnimationUsingKeyFrames>
                            </Storyboard>
                        </VisualState>
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Pressed">
                            <Storyboard>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Content" Storyboard.TargetProperty="(TextBlock.Foreground)">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource Theme_BackgroundBrush}"/>
                                </ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                        </VisualState>
                        <VisualState x:Name="Disabled" />
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
                <Ellipse x:Name="FocusVisual" Margin="-3"
                                  IsHitTestVisible="false"
                                  Visibility="Collapsed"
                                  Stroke="{TemplateBinding Foreground}" StrokeThickness="2" StrokeDashArray="5.5,5"/>
                <Ellipse x:Name="Hover" Fill="Transparent"/>
                <ContentPresenter x:Name="Content" Margin="4" Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </ControlTemplate>
        <Style TargetType="TextBox">
            <Setter Property="Height"
            Value="32" />
            <Setter Property="Padding"
            Value="8,2,8,2" />
            <Setter Property="Foreground"
            Value="{DynamicResource Theme_TextBrush}" />
            <Setter Property="Background"
            Value="{DynamicResource Theme_ControlBackgroundBrush}" />
            <Setter Property="BorderBrush"
            Value="{DynamicResource Theme_PrimaryBrush}" />
            <Setter Property="CaretBrush"
            Value="{DynamicResource Theme_TextBrush}" />
            <Setter Property="BorderThickness"
            Value="1" />
            <Setter Property="VerticalContentAlignment"
            Value="Center" />
            <Setter Property="FontSize"
            Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid x:Name="RootElement">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="MouseOver">
                                        <Storyboard>
                                            <DoubleAnimationUsingKeyFrames Duration="00:00:00.001"
                                                                   Storyboard.TargetName="BorderColor"
                                                                   Storyboard.TargetProperty="(UIElement.Opacity)">
                                                <SplineDoubleKeyFrame KeyTime="0"
                                                              Value="1" />
                                            </DoubleAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                    </VisualState>
                                    <VisualState x:Name="ReadOnly" />
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="FocusStates">
                                    <VisualState x:Name="Focused">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Duration="00:00:00.001"
                                                                   Storyboard.TargetName="HasFocus"
                                                                   Storyboard.TargetProperty="(UIElement.Visibility)">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                Value="Visible" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Unfocused" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="Background"
                            CornerRadius="15"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}"
                            Opacity="0.5">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="3" Color="Black" BlurRadius="8" Opacity="0.3" />
                                </Border.Effect>
                            </Border>
                            <Border x:Name="BorderColor"
                            CornerRadius="15"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="Transparent"
                            Opacity="0" />
                            <Border x:Name="HasFocus"
                            CornerRadius="15"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="Transparent"
                            Visibility="Collapsed">
                            </Border>

                            <ScrollViewer x:Name="ContentElement"
                                  BorderThickness="0"
                                  IsTabStop="False"
                                  Margin="{TemplateBinding Padding}"
                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                  Foreground="{TemplateBinding Foreground}" />

                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="AutoCompleteBox">
            <Setter Property="Height"
            Value="32" />
            <Setter Property="IsTabStop"
            Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="AutoCompleteBox">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="PopupStates">
                                    <VisualStateGroup.Transitions>
                                        <VisualTransition GeneratedDuration="0:0:0.1"
                                                      To="PopupOpened" />
                                        <VisualTransition GeneratedDuration="0:0:0.2"
                                                      To="PopupClosed" />
                                    </VisualStateGroup.Transitions>
                                    <VisualState x:Name="PopupOpened">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Selector"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="1.0" />
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="PopupClosed">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Selector"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="0.0" />
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <TextBox x:Name="Text"
                                     IsTabStop="True" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}"
                                     PlaceholderText="Search Samples"
                                     />
                            <Popup x:Name="Popup">
                                <ListBox x:Name="Selector"
                                 Opacity="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 ItemTemplate="{TemplateBinding ItemTemplate}"
                                 DisplayMemberPath="{TemplateBinding ValueMemberPath}" />
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <AutoCompleteBox x:Name="SearchField" ItemsSource="{x:Static local:ControlSearch.SearchKeywords}" FilterMode="Contains" MinimumPopulateDelay="500"
                         MinimumPrefixLength="2" MinWidth="40" Height="32" Margin="0,0,5,0" BorderBrush="Transparent" Background="{DynamicResource Theme_BackgroundBrush}" Padding="9,0,36,0"/>
        <Button Click="ButtonSearch_Click"  Width="30" Height="26" Margin="0,0,8,0"
                Background="{DynamicResource Theme_ContainerBackgroundBrush}"
                Foreground="{DynamicResource Theme_TextBrush}"
                HorizontalAlignment="Right"
                Template="{StaticResource CircleButtonTemplate}">
            <Path Stroke="{DynamicResource Theme_TextBrush}" Data="M 17 17 A 1 1 0 0 0 -17 -17 A 1 1 0 0 0 17 17 L 52 54" StrokeThickness="2" Stretch="Uniform"/>
        </Button>
        <TextBlock Text="Search"
           x:Name="HintTextBlock"
           IsHitTestVisible="False"
           Visibility="{Binding Text, ElementName=SearchField, Converter={StaticResource StringEmptyToVisibilityConverter}}"
           Foreground="{DynamicResource Theme_TextBrush}"
           HorizontalAlignment="Left" VerticalAlignment="Center" Margin="11,0,0,0" FontSize="14"/>
    </Grid>
</UserControl>
