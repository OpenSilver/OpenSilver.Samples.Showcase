<sdk:Page
x:Class="OpenSilver.Samples.Showcase.MainPage"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:local="clr-namespace:OpenSilver.Samples.Showcase"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
xmlns:animations="clr-namespace:OpenSilver.Animations;assembly=OpenSilver.Animations" xmlns:easing="clr-namespace:OpenSilver.Animations.Easing;assembly=OpenSilver.Animations"
    Background="White"
mc:Ignorable="d"
    Foreground="{DynamicResource Theme_TextBrush}"
    FontFamily="{StaticResource InterFontFamily}">
    <Page.Resources>
        <SolidColorBrush x:Key="NativeApiButtonBackground" Color="#dddddd"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource Theme_TextBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton" xmlns:system="clr-namespace:System;assembly=mscorlib">
                        <Grid Margin="{TemplateBinding Padding}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <!--<VisualStateGroup.Transitions>
                                        <VisualTransition GeneratedDuration="00:00:00.1"
                                                          To="MouseOver" />
                                        <VisualTransition From="MouseOver"
                                                          GeneratedDuration="00:00:00.1" />
                                    </VisualStateGroup.Transitions>-->
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="MouseOver">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             To="1.05"
                                                             Duration="00:00:00.1"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             To="1.05"
                                                             Duration="00:00:00.1"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             To="0.95"
                                                             Duration="00:00:00.1"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             To="0.95"
                                                             Duration="00:00:00.1"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled"/>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Duration="00:00:00.001"
                                                                       Storyboard.TargetName="border"
                                                                       Storyboard.TargetProperty="(UIElement.Background)">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                                        Value="{DynamicResource Theme_ContainerBackgroundBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Duration="00:00:00.001"
                                                             Storyboard.TargetName="border"
                                                             Storyboard.TargetProperty="(UIElement.Effect)">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                             Value="{x:Null}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="FocusStates">
                                    <VisualState x:Name="Focused" />
                                    <VisualState x:Name="Unfocused" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="border" Background="Transparent" CornerRadius="5" RenderTransformOrigin="0.5,0.5">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="3" Color="Black" BlurRadius="5" Opacity="0.3" />
                                </Border.Effect>
                                <Border.RenderTransform>
                                    <ScaleTransform x:Name="scaleTransform"/>
                                </Border.RenderTransform>
                                <ContentControl x:Name="contentPresenter"
                                                Foreground="{DynamicResource Theme_TextBrush}"
                                                Margin="8,6,8,6"
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="RowThatContainsTheBanner" Height="Auto"/>
            <RowDefinition x:Name="RowThatContainsThePage" Height="1*"/>
            <RowDefinition x:Name="RowThatContainsTheGridSplitter" Height="0"/>
            <RowDefinition x:Name="RowThatContainsTheSourceCodePane" Height="0*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" ClipToBounds="True" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ColumnThatContainsTheMenu" Width="Auto"/>
                <ColumnDefinition x:Name="ColumnThatContainsThePage" Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Background -->
            <Grid x:Name="BackgroundImagesContainer" Grid.ColumnSpan="2" animations:Animation.OnAppear="{animations:Fade Delay=1000, Duration=1000}">
                <Image Source="/Other/Images/BackgroundImageDark.jpg" x:Name="BackgroundImageDark" Opacity="0" Stretch="Fill" />
                <Image Source="/Other/Images/BackgroundImageLight.jpg" x:Name="BackgroundImageLight" Stretch="Fill" />
            </Grid>

            <!-- Menu -->
            <animations:AnimatedContentControl x:Name="MenuContainer" Canvas.ZIndex="3" animations:Animation.OnAppear="{animations:Fade Delay=4000, Duration=2000, Easing=CubicInOut}" HorizontalAlignment="Left">
                <Border x:Name="MenuBorder" Canvas.ZIndex="1" Width="240" Background="{DynamicResource Theme_BackgroundBrush}">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="3" Color="Black" BlurRadius="14" Opacity="0.15" />
                    </Border.Effect>
                    <Border.RenderTransform>
                        <TranslateTransform/>
                    </Border.RenderTransform>
                    <DockPanel LastChildFill="True">
                        <Border DockPanel.Dock="Bottom" Margin="0,10,0,20" Padding="3" CornerRadius="7" HorizontalAlignment="Center" Background="{DynamicResource Theme_BackgroundBrush}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="LightThemeRadioButton" IsChecked="True" Checked="ThemeToggle_RadioButton_Checked" animations:SpringEffect.IsEnabled="True">
                                    <StackPanel Orientation="Horizontal">
                                        <local:SvgImage x:Name="lightThemeImage" Width="20" Height="20" Source="/Other/Icons/LightTheme.svg"/>
                                        <TextBlock Margin="3" VerticalAlignment="Center" FontSize="11" Text="Light" Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=ContentControl}}"/>
                                    </StackPanel>
                                </RadioButton>
                                <RadioButton x:Name="DarkThemeRadioButton" Margin="4,0,0,0" Checked="ThemeToggle_RadioButton_Checked" animations:SpringEffect.IsEnabled="True">
                                    <StackPanel Orientation="Horizontal">
                                        <local:SvgImage x:Name="darkThemeImage" Width="20" Height="20" Source="/Other/Icons/DarkTheme.svg"/>
                                        <TextBlock Margin="3" VerticalAlignment="Center" FontSize="11" Text="Dark" Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=ContentControl}}"/>
                                    </StackPanel>
                                </RadioButton>
                            </StackPanel>
                        </Border>
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0">
                            <StackPanel >
                                <Grid Margin="50,32,55,0" HorizontalAlignment="Stretch" animations:SpringEffect.IsEnabled="True">
                                    <Image Source="/OpenSilver.Samples.Showcase;component/Other/Images/LogoOpenSilverDark.png" x:Name="LogoOpenSilverDark" Opacity="0" Cursor="Hand" MouseLeftButtonDown="Logo_MouseLeftButtonDown" />
                                    <Image Source="/OpenSilver.Samples.Showcase;component/Other/Images/LogoOpenSilverLight.png" x:Name="LogoOpenSilverLight" Cursor="Hand" MouseLeftButtonDown="Logo_MouseLeftButtonDown" />
                                </Grid>
                                <Border HorizontalAlignment="Center" VerticalAlignment="Bottom" Padding="6,1,6,2" Margin="0,2,0,4" CornerRadius="10" Cursor="Hand" MouseLeftButtonDown="Logo_MouseLeftButtonDown" Background="Transparent" animations:SpringEffect.IsEnabled="True">
                                    <TextBlock Text="WPF FOR THE MODERN WEB" Foreground="{DynamicResource Theme_TextBrush}" Opacity="0.7" FontWeight="Bold" FontSize="10"/>
                                </Border>
                                <Image Source="/OpenSilver.Samples.Showcase;component/Other/Images/logo-showcase.png" Margin="75,20,75,0" Cursor="Hand" MouseLeftButtonDown="Logo_MouseLeftButtonDown" animations:SpringEffect.IsEnabled="True"/>
                                <local:SearchControl Margin="32,40,30,20" animations:SpringEffect.IsEnabled="True"/>
                                <ListBox x:Name="MenuListBox" SelectionChanged="MenuListBox_SelectionChanged" Background="Transparent" Margin="30,0,0,0" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Hidden">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <Border Background="Transparent"
                                                                Visibility="{Binding IsVisibleInMenu, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                animations:SpringEffect.IsEnabled="True">
                                                            <VisualStateManager.VisualStateGroups>
                                                                <VisualStateGroup x:Name="CommonStates">
                                                                    <VisualState x:Name="Normal" />
                                                                </VisualStateGroup>
                                                                <VisualStateGroup x:Name="SelectionStates">
                                                                    <VisualState x:Name="Unselected"/>
                                                                    <VisualState x:Name="Selected">
                                                                        <Storyboard>
                                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectedBorder" 
                                                                                                    Storyboard.TargetProperty="(Border.Background)" 
                                                                                                    Duration="0">
                                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource Theme_PrimaryBrush}"/>
                                                                            </ObjectAnimationUsingKeyFrames>
                                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContainerBorder" 
                                                                                                    Storyboard.TargetProperty="(TextBlock.Foreground)" 
                                                                                                    Duration="0">
                                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource Theme_TextOnPrimaryBrush}"/>
                                                                            </ObjectAnimationUsingKeyFrames>
                                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContainerBorder"
                                                                                                    Storyboard.TargetProperty="(TextBlock.FontWeight)"
                                                                                                    Duration="0">
                                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="SemiBold" />
                                                                            </ObjectAnimationUsingKeyFrames>
                                                                        </Storyboard>
                                                                    </VisualState>
                                                                </VisualStateGroup>
                                                            </VisualStateManager.VisualStateGroups>
                                                            <Border x:Name="SelectedBorder" Background="Transparent" CornerRadius="20" HorizontalAlignment="Left">
                                                                <Border x:Name="ContainerBorder" Background="Transparent" Grid.Column="1" Margin="10,5,10,5" TextBlock.FontSize="16" TextBlock.Foreground="{DynamicResource Theme_TextBrush}">
                                                                    <ContentPresenter x:Name="Presenter" TextBlock.Foreground="{Binding (TextBlock.Foreground), ElementName=ContainerBorder}"/>
                                                                </Border>
                                                            </Border>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Cursor="Hand">
                                                <TextBlock Text="{Binding Name}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </Border>
            </animations:AnimatedContentControl>
            <Border x:Name="ButtonToHideOrShowMenu" Grid.Column="1" Canvas.ZIndex="2" Padding="2" Visibility="Collapsed" Width="60" Height="50" Margin="5" VerticalAlignment="Top" HorizontalAlignment="Left">
                <Button Click="ButtonToHideOrShowMenu_Click" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent" Padding="0">
                    <Path Fill="{DynamicResource Theme_TextBrush}" Opacity="0.5" Width="46" Height="46" Data="M10 0A1 1 90 0010 15L90 15A1 1 90 0089 0L10 0M10 22.5A1 1 90 0010 37.5L90 37.5A1 1 90 0089 22.5L10 22.5M10 45A1 1 90 0010 60L90 60A1 1 90 0089 45L10 45" Stretch="Uniform" HorizontalAlignment="Center"/>
                </Button>
            </Border>

            <!-- Page -->
            <ScrollViewer x:Name="PageScrollViewer" Grid.Column="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Padding="0">
                <sdk:Frame x:Name="PageContainer" JournalOwnership="Automatic" HorizontalContentAlignment="Stretch" Navigated="PageContainer_Navigated">
                    <sdk:Frame.UriMapper>
                        <sdk:UriMapper>
                            <sdk:UriMapping Uri="/Welcome" MappedUri="/Samples/Welcome.xaml"/>
                            <sdk:UriMapping Uri="/Search" MappedUri="/Samples/SearchPage.xaml"/>
                            <sdk:UriMapping Uri="/Search/{searchTerms}" MappedUri="/Samples/SearchPage.xaml?SearchTerms={searchTerms}"/>
                            <sdk:UriMapping Uri="/{page_name}/{demo_name}" MappedUri="/Samples/{page_name}/{demo_name}/{demo_name}_Demo.xaml"/>
                            <sdk:UriMapping Uri="/{page_name}" MappedUri="/Samples/{page_name}/{page_name}.xaml"/>
                        </sdk:UriMapper>
                    </sdk:Frame.UriMapper>
                    <sdk:Frame.RenderTransform>
                        <TranslateTransform/>
                    </sdk:Frame.RenderTransform>
                </sdk:Frame>
            </ScrollViewer>
        </Grid>
        <GridSplitter Grid.Row="2" x:Name="GridSplitter1" Background="LightGray" Visibility="Collapsed" Cursor="SizeNS" Height="5" HorizontalAlignment="Stretch"/>
        <Grid Grid.Row="3" x:Name="SourceCodePane" Visibility="Collapsed" Background="{DynamicResource Theme_BackgroundBrush}">
            <TextBlock Text="Loading source code..." FontSize="14" Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap"/>
            <Border x:Name="PlaceWhereSourceCodeWillBeDisplayed" Margin="0,10,0,0"/>
            <Button x:Name="ButtonToCloseSourceCode" Content="×" FontSize="20" FontWeight="Bold" Foreground="LightGray" Background="#FF6C6C6C" HorizontalAlignment="Right" HorizontalContentAlignment="Center" VerticalAlignment="Top" VerticalContentAlignment="Center" Padding="5,-2,4,2" Margin="5" Click="ButtonToCloseSourceCode_Click" />
        </Grid>
    </Grid>
</sdk:Page>
