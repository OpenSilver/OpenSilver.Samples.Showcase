<sdk:Page
x:Class="OpenSilver.Showcase.MainPage"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:local="clr-namespace:OpenSilver.Showcase"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
xmlns:animations="clr-namespace:OpenSilver.Animations;assembly=OpenSilver.Animations"
Background="White"
mc:Ignorable="d"
    Foreground="{DynamicResource Theme_TextBrush}"
    FontFamily="{StaticResource InterFontFamily}" d:DesignWidth="430" d:DesignHeight="932">
    <Page.Resources>
        <SolidColorBrush x:Key="NativeApiButtonBackground" Color="#dddddd"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <animations:Fade x:Key="MenuAnimation_OnAppear_Default" Delay="4500" Duration="1000" EasingMode="EaseOut"/>
        <animations:Fade x:Key="MenuAnimation_OnAppear_Faster" Delay="0" Duration="500" EasingMode="EaseOut"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="RowThatContainsTheBanner" Height="Auto"/>
            <RowDefinition x:Name="RowThatContainsThePage" Height="1*"/>
            <RowDefinition x:Name="RowThatContainsTheGridSplitter" Height="0"/>
            <RowDefinition x:Name="RowThatContainsTheSourceCodePane" Height="0*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" ClipToBounds="True" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ColumnThatContainsTheMenu" Width="Auto"/>
                <ColumnDefinition x:Name="ColumnThatContainsThePage" Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Background -->
            <Grid x:Name="BackgroundImagesContainer" Grid.ColumnSpan="2" animations:Animation.OnAppear="{animations:Fade Delay=1000, Duration=1000}">
                <Image Source="/Other/Images/BackgroundImageDark.jpg" x:Name="BackgroundImageDark" Opacity="0" Stretch="Fill" />
                <Image Source="/Other/Images/BackgroundImageLight.jpg" x:Name="BackgroundImageLight" Stretch="Fill" />
            </Grid>

            <!-- Menu -->
            <animations:AnimatedContentControl x:Name="MenuContainer" Canvas.ZIndex="3" animations:Animation.OnAppear="{StaticResource MenuAnimation_OnAppear_Default}" HorizontalAlignment="Left">
                <Border x:Name="MenuBorder" Canvas.ZIndex="1" Width="240" Background="{DynamicResource Theme_BackgroundBrush}">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="3" Color="Black" BlurRadius="14" Opacity="0.15" />
                    </Border.Effect>
                    <Border.RenderTransform>
                        <TranslateTransform/>
                    </Border.RenderTransform>
                    <DockPanel LastChildFill="True">
                        <HyperlinkButton x:Name="OpenSilverLogoButton" DockPanel.Dock="Bottom" NavigateUri="https://opensilver.net" Margin="0,0,0,6" Padding="0" TargetName="_blank" Cursor="Hand" HorizontalAlignment="Center" animations:SpringEffect.IsEnabled="True">
                            <Grid>
                                <!--<Border Opacity="0.3" CornerRadius="15">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="3" Color="Black" BlurRadius="8" Opacity="0.5" />
                                    </Border.Effect>
                                </Border>-->
                                <StackPanel>
                                    <TextBlock Text="MADE WITH" Margin="0,5,0,0" Foreground="{StaticResource MainGradientBrush}" CharacterSpacing="70" Opacity="0.7" FontWeight="Bold" FontSize="11" HorizontalAlignment="Center" />
                                    <Grid Margin="0,0,0,0" Width="130" HorizontalAlignment="Stretch">
                                        <Image Source="/OpenSilver.Showcase;component/Other/Images/LogoOpenSilverDark.png" x:Name="LogoOpenSilverDark" Opacity="0"/>
                                        <Image Source="/OpenSilver.Showcase;component/Other/Images/LogoOpenSilverLight.png" x:Name="LogoOpenSilverLight"/>
                                    </Grid>
                                    <Border HorizontalAlignment="Center" VerticalAlignment="Bottom" Padding="10,1,10,4" Margin="0,3,0,4" CornerRadius="10" Cursor="Hand" MouseLeftButtonDown="Logo_MouseLeftButtonDown" Background="Transparent">
                                        <TextBlock Text="WPF Evolved &amp; Cross-Platform" Foreground="{DynamicResource Theme_TextBrush}" Opacity="0.6" FontWeight="Bold" FontSize="11" RenderTransformOrigin="0.5,0.5">
                                            <TextBlock.RenderTransform>
                                                <ScaleTransform ScaleX="0.9"/>
                                            </TextBlock.RenderTransform>
                                        </TextBlock>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </HyperlinkButton>
                        <Border DockPanel.Dock="Bottom" Margin="0,5,0,8" Padding="3" CornerRadius="7" HorizontalAlignment="Center" Background="{DynamicResource Theme_BackgroundBrush}">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="LightThemeRadioButton" IsChecked="True" Checked="ThemeToggle_RadioButton_Checked" Style="{StaticResource LightDarkToggle_RadioButton_Style}" animations:SpringEffect.IsEnabled="True">
                                    <StackPanel Orientation="Horizontal">
                                        <local:SvgImage x:Name="lightThemeImage" Width="20" Height="20" Source="/Other/Icons/LightTheme.svg" FillColor="{Binding Path=Foreground.(SolidColorBrush.Color), RelativeSource={RelativeSource AncestorType=ContentControl}}"/>
                                        <TextBlock Margin="3" VerticalAlignment="Center" FontSize="11" FontWeight="Bold" Text="Light" Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=ContentControl}}"/>
                                    </StackPanel>
                                </RadioButton>
                                <RadioButton x:Name="DarkThemeRadioButton" Margin="4,0,0,0" Checked="ThemeToggle_RadioButton_Checked" Style="{StaticResource LightDarkToggle_RadioButton_Style}" animations:SpringEffect.IsEnabled="True">
                                    <StackPanel Orientation="Horizontal">
                                        <local:SvgImage x:Name="darkThemeImage" Width="20" Height="20" Source="/Other/Icons/DarkTheme.svg" FillColor="{Binding Path=Foreground.(SolidColorBrush.Color), RelativeSource={RelativeSource AncestorType=ContentControl}}"/>
                                        <TextBlock Margin="3" VerticalAlignment="Center" FontSize="11" FontWeight="Bold" Text="Dark" Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=ContentControl}}"/>
                                    </StackPanel>
                                </RadioButton>
                            </StackPanel>
                        </Border>
                        <Rectangle x:Name="HorizontalSeparatorThinBar" DockPanel.Dock="Bottom" Height="1" Margin="0,0,0,6" Fill="{DynamicResource Theme_TextBrush}" Opacity="0.3" HorizontalAlignment="Stretch"/>
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0">
                            <StackPanel>
                                <Grid Margin="15,15,15,0" HorizontalAlignment="Stretch" Cursor="Hand" MouseLeftButtonDown="Logo_MouseLeftButtonDown" animations:SpringEffect.IsEnabled="True">
                                    <Image Source="/OpenSilver.Showcase;component/Other/Images/LogoShowcaseDark.png" x:Name="LogoShowcaseDark" Opacity="0"/>
                                    <Image Source="/OpenSilver.Showcase;component/Other/Images/LogoShowcaseLight.png" x:Name="LogoShowcaseLight"/>
                                </Grid>
                                <local:SearchControl Margin="21,24,18,0" animations:SpringEffect.IsEnabled="True"/>
                                <TreeView
                                    x:Name="MenuTreeView"
                                    Margin="20,10,0,0"
                                    Style="{StaticResource Menu_TreeView_Style}"
                                    SelectedItemChanged="MenuTreeView_SelectedItemChanged"/>
                                <HyperlinkButton x:Name="GooglePlayBadgeButton" Width="120" NavigateUri="https://play.google.com/store/apps/details?id=net.opensilver.showcase" Margin="0,20,0,0" Padding="0" TargetName="_blank" Cursor="Hand" HorizontalAlignment="Center" animations:SpringEffect.IsEnabled="True">
                                    <Grid>
                                        <Image Source="/OpenSilver.Showcase;component/Other/Images/GooglePlayBadgeDark.png" x:Name="GooglePlayBadgeDark" Opacity="0"/>
                                        <Image Source="/OpenSilver.Showcase;component/Other/Images/GooglePlayBadgeLight.png" x:Name="GooglePlayBadgeLight"/>
                                    </Grid>
                                </HyperlinkButton>
                                <HyperlinkButton x:Name="AppStoreBadgeButton" Width="120" NavigateUri="https://apps.apple.com/app/opensilver-showcase/id6746472943" Margin="0,10,0,20" Padding="0" TargetName="_blank" Cursor="Hand" HorizontalAlignment="Center" animations:SpringEffect.IsEnabled="True">
                                    <Grid Cursor="Hand" animations:SpringEffect.IsEnabled="True">
                                        <Image Source="/OpenSilver.Showcase;component/Other/Images/AppStoreBadgeDark.png" x:Name="AppStoreBadgeDark" Opacity="0"/>
                                        <Image Source="/OpenSilver.Showcase;component/Other/Images/AppStoreBadgeLight.png" x:Name="AppStoreBadgeLight"/>
                                    </Grid>
                                </HyperlinkButton>
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </Border>
            </animations:AnimatedContentControl>
            <Border x:Name="ButtonToHideOrShowMenu" Grid.Column="1" Canvas.ZIndex="2" Padding="2" Visibility="Collapsed" Width="60" Height="50" Margin="5" VerticalAlignment="Top" HorizontalAlignment="Left"
                    animations:Animation.OnAppear="{animations:Fade Delay=100, Duration=500, EasingMode=EaseInOut}">
                <Button Click="ButtonToHideOrShowMenu_Click" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent" Padding="0">
                    <Path Fill="{DynamicResource Theme_TextBrush}" Opacity="0.5" Width="46" Height="46" Data="M10 0A1 1 90 0010 15L90 15A1 1 90 0089 0L10 0M10 22.5A1 1 90 0010 37.5L90 37.5A1 1 90 0089 22.5L10 22.5M10 45A1 1 90 0010 60L90 60A1 1 90 0089 45L10 45" Stretch="Uniform" HorizontalAlignment="Center"/>
                </Button>
            </Border>

            <!-- Page -->
            <ScrollViewer x:Name="PageScrollViewer" Grid.Column="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Padding="0">
                <Grid>
                    <sdk:Frame x:Name="PageContainer" JournalOwnership="Automatic" HorizontalContentAlignment="Stretch" Navigated="PageContainer_Navigated">
                        <sdk:Frame.UriMapper>
                            <sdk:UriMapper>
                                <sdk:UriMapping Uri="/Welcome" MappedUri="/Samples/Welcome.xaml"/>
                                <sdk:UriMapping Uri="/Search" MappedUri="/Samples/SearchPage.xaml"/>
                                <sdk:UriMapping Uri="/Search/{searchTerms}" MappedUri="/Samples/SearchPage.xaml?SearchTerms={searchTerms}"/>
                                <sdk:UriMapping Uri="/{page_name}/{demo_name}" MappedUri="/Samples/{page_name}/{demo_name}/{demo_name}_Demo.xaml"/>
                                <sdk:UriMapping Uri="/{page_name}" MappedUri="/Samples/{page_name}/{page_name}.xaml"/>
                            </sdk:UriMapper>
                        </sdk:Frame.UriMapper>
                    </sdk:Frame>
                    <HyperlinkButton x:Name="ForkOnGitHubButton"
                                     NavigateUri="https://github.com/OpenSilver/OpenSilver.Samples.Showcase"
                                     TargetName="_blank"
                                     Cursor="Hand"
                                     Opacity="0"
                                     animations:Animation.OnAppear="{animations:FadeAndSlide Delay=2000, Duration=500, Direction=UpToDown, EasingMode=EaseOut}"
                                     HorizontalAlignment="Right"
                                     VerticalAlignment="Top"
                                     Margin="0,-6,20,0">
                        <Border 
                            Padding="14,8,14,8"
                            CornerRadius="0,0,10,10"
                            Background="{DynamicResource Theme_ControlBackgroundBrush}"
                            animations:SpringEffect.IsEnabled="True">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="3" Color="Black" BlurRadius="14" Opacity="0.15"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="FORK ME ON" Foreground="{DynamicResource Theme_TextBrush}" Opacity="0.5" FontSize="8" FontWeight="Bold" CharacterSpacing="80"/>
                                <Grid Opacity="0.7" Margin="0,2,0,0">
                                    <Image x:Name="LogoGitHubDark" Source="/OpenSilver.Showcase;component/Other/Images/LogoGitHubDark.png" Width="70" Opacity="0"/>
                                    <Image x:Name="LogoGitHubLight" Source="/OpenSilver.Showcase;component/Other/Images/LogoGitHubLight.png" Width="70"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </HyperlinkButton>
                    <HyperlinkButton x:Name="SuggestSamplesButton"
                                     NavigateUri="https://feedback.opensilvershowcase.com/"
                                     TargetName="_blank"
                                     Cursor="Hand"
                                     Opacity="0"
                                     animations:Animation.OnAppear="{animations:FadeAndSlide Delay=2300, Duration=500, Direction=UpToDown, EasingMode=EaseOut}"
                                     HorizontalAlignment="Right"
                                     VerticalAlignment="Top"
                                     Margin="0,-6,130,0">
                        <Border 
                            Padding="12,10,12,8"
                            CornerRadius="0,0,10,10"
                            Background="{DynamicResource Theme_ControlBackgroundBrush}"
                            animations:SpringEffect.IsEnabled="True">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="3" Color="Black" BlurRadius="14" Opacity="0.15"/>
                            </Border.Effect>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="" FontFamily="{StaticResource MaterialIconsRegular_FontFamily}" FontSize="18" Margin="0,1,0,0"/>
                                    <TextBlock Text="Suggest" Foreground="{DynamicResource Theme_TextBrush}" Opacity="0.7" Margin="6,0,0,0" FontSize="14" FontWeight="Bold"/>
                                </StackPanel>
                                <TextBlock Text="NEW SAMPLES" Foreground="{DynamicResource Theme_TextBrush}" Opacity="0.5" Margin="0,-2,0,0" FontSize="9" FontWeight="Bold" CharacterSpacing="80" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </HyperlinkButton>
                </Grid>
                <ScrollViewer.RenderTransform>
                    <TranslateTransform/>
                </ScrollViewer.RenderTransform>
            </ScrollViewer>
        </Grid>
        <GridSplitter Grid.Row="2" x:Name="GridSplitter1" Background="LightGray" Visibility="Collapsed" Cursor="SizeNS" Height="5" HorizontalAlignment="Stretch"/>
        <Grid Grid.Row="3" x:Name="SourceCodePane" Visibility="Collapsed" Background="{DynamicResource Theme_BackgroundBrush}">
            <TextBlock Text="Loading source code..." FontSize="14" Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap"/>
            <Border x:Name="PlaceWhereSourceCodeWillBeDisplayed" Margin="0,10,0,0"/>
            <Button x:Name="ButtonToCloseSourceCode" Content="×" FontSize="20" FontWeight="Bold" Foreground="LightGray" Background="#FF6C6C6C" HorizontalAlignment="Right" HorizontalContentAlignment="Center" VerticalAlignment="Top" VerticalContentAlignment="Center" Padding="5,-2,4,2" Margin="5" Click="ButtonToCloseSourceCode_Click" />
        </Grid>
    </Grid>
</sdk:Page>
