<Page x:Class="OpenSilver.Showcase.Charts"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controlskit="clr-namespace:OpenSilver.ControlsKit;assembly=OpenSilver.ControlsKit.Controls"
             xmlns:local="clr-namespace:OpenSilver.Showcase"
             Style="{StaticResource Page_Style}"
             Title="Charts">
    
    <StackPanel>
        <StackPanel>
            <ContentControl Content="Charts" Style="{StaticResource PageTitle_Style}"/>
            <TextBlock Style="{StaticResource PageIntroText_Style}">OpenSilver includes a rich set of built-in <Hyperlink NavigateUri="https://github.com/OpenSilver/OpenSilver/tree/develop/src/Runtime/Controls.DataVisualization" TargetName="_blank" Foreground="{DynamicResource Theme_TextBrush}">open-source</Hyperlink>
                                                                    chart components, covering common types such as line, bar, pie, area,
                                                                    scatter, and more. They are fully customizable through XAML and support
                                                                    data binding.
            </TextBlock>
        </StackPanel>

        <ItemsControl x:Name="SamplesPanel" local:ItemsControlLoadingBehavior.ShowLoadingOnGenerating="True">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <controlskit:StaggeredPanel DesiredColumnWidth="700" ProgressiveRenderingChunkSize="2"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <local:AreaSeries_Demo HorizontalAlignment="Center"/>
            <local:BarSeries_Demo HorizontalAlignment="Center"/>
            <local:BubbleSeries_Demo HorizontalAlignment="Center"/>
            <local:ColumnSeries_Demo HorizontalAlignment="Center"/>
            <local:LineSeries_Demo HorizontalAlignment="Center"/>
            <local:PieSeries_Demo HorizontalAlignment="Center"/>
            <local:ScatterSeries_Demo HorizontalAlignment="Center"/>
            <local:Stacked100AreaSeries_Demo HorizontalAlignment="Center"/>
        </ItemsControl>
    </StackPanel>
</Page>
