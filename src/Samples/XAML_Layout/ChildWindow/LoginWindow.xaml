<ChildWindow x:Class="PreviewOnWinRT.LoginWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           Width="300" Height="250" 
           Title="LoginWindow"
           Closing="LoginWindow_Closing"
           >
    <Grid x:Name="LayoutRoot" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Name" />
            <TextBox x:Name="nameBox"/>
            <TextBlock Text="Password" />
            <PasswordBox x:Name="passwordBox"/>
        </StackPanel>

        <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Width="75" HorizontalAlignment="Right" Margin="0,12,0,0" Grid.Row="1"
                Foreground="{DynamicResource Theme_PrimaryBrush}" Background="{DynamicResource Theme_ContainerBackgroundBrush}" />
        <Button x:Name="OKButton" Content="OK" Click="OKButton_Click" Width="75" HorizontalAlignment="Right" Margin="0,12,79,0" Grid.Row="1" />
    </Grid>
</ChildWindow>