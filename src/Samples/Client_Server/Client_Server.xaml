<Page x:Class="OpenSilver.Samples.Showcase.Client_Server"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controlskit="clr-namespace:OpenSilver.ControlsKit;assembly=OpenSilver.ControlsKit.Controls"
             xmlns:local="clr-namespace:OpenSilver.Samples.Showcase"
             Style="{StaticResource Page_Style}"
             Title="Client / Server">

    <StackPanel>
        <StackPanel>
            <ContentControl Content="Client / Server" Style="{StaticResource PageTitle_Style}"/>
            <TextBlock Style="{StaticResource PageIntroText_Style}">OpenSilver makes it easy to connect with server APIs using REST calls via
                                                                    HttpClient, WCF SOAP services through "Add Service Reference", WebSockets
                                                                    for real-time communication, and RIA Services for advanced scenarios.
                                                                    Modern browser security requires <Hyperlink NavigateUri="https://doc.opensilver.net/documentation/in-depth-topics/wcf-and-webclient.html#adding-support-for-cross-domain-calls-cors" TargetName="_blank" Foreground="{DynamicResource Theme_TextBrush}">enabling CORS</Hyperlink>
                                                                    and configuring the SameSite cookie flag on your server to ensure smooth
                                                                    cross-origin communication.
            </TextBlock>
        </StackPanel>

        <ItemsControl x:Name="SamplesPanel" local:ItemsControlLoadingBehavior.ShowLoadingOnGenerating="True">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <controlskit:StaggeredPanel DesiredColumnWidth="320" ProgressiveRenderingChunkSize="2"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <local:WCF_SOAP_Demo HorizontalAlignment="Center"/>
            <local:REST_HttpClient_Demo x:Name="REST_WebClientDemo" HorizontalAlignment="Center"/>
            <local:WebSockets_Demo HorizontalAlignment="Center"/>
            <local:RIAServices_Demo HorizontalAlignment="Center"/>
            <local:JSONP_Demo HorizontalAlignment="Center"/>
            
        </ItemsControl>
    </StackPanel>
</Page>
