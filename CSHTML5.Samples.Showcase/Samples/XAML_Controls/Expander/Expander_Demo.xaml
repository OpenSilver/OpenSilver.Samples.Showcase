<UserControl
    x:Class="CSHTML5.Samples.Showcase.Expander_Demo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:local="using:CSHTML5.Samples.Showcase">
    <ContentControl Style="{StaticResource Card_Style}" Height="140" Width="220">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ContentControl Content="Expander" Foreground="#FCAB54" Background="#FFCCA376" Style="{StaticResource HeaderControl_Style}"/>
            <Expander Grid.Row="1" Header="Click to Expand/Collapse" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="This is the content of the Expander" Margin="30,20,20,20"/>
            </Expander>
            <!--<Button Grid.Row="2" Content="MORE DEMOS..." Click="ButtonViewMore_Click" Background="#FCAB54" Style="{StaticResource ButtonViewMore_Style}"/>-->
        </Grid>
    </ContentControl>
</UserControl>
